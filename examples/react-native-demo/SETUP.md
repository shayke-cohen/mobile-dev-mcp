# React Native Demo - Setup Instructions

This demo app has all source code complete. Follow these steps to initialize the native projects:

## Quick Setup

### Option 1: Create Fresh RN Project and Copy Source (Recommended)

```bash
# 1. Create a new React Native project
npx react-native init MCPDemoApp --template react-native-template-typescript

# 2. Copy the source files from this demo
cp -r src/* MCPDemoApp/

# 3. Install additional dependencies
cd MCPDemoApp
npm install @reduxjs/toolkit react-redux @react-navigation/native @react-navigation/native-stack react-native-screens react-native-safe-area-context @react-native-async-storage/async-storage

# 4. Install iOS pods
cd ios && pod install && cd ..

# 5. Run the app
npm run ios
# or
npm run android
```

### Option 2: Initialize in Place

```bash
# 1. Navigate to this directory
cd examples/react-native-demo

# 2. Initialize native projects (this may overwrite some files)
npx react-native init MCPDemoApp --directory .

# 3. Restore our source files if overwritten
# (git checkout src/ if using version control)

# 4. Install pods for iOS
cd ios && pod install && cd ..
```

## After Setup

1. **Start Metro bundler:**
   ```bash
   npm start
   ```

2. **Run on iOS:**
   ```bash
   npm run ios
   ```

3. **Run on Android:**
   ```bash
   npm run android
   ```

4. **Start MCP server (in another terminal):**
   ```bash
   cd ../../packages/mcp-server
   npm run dev
   ```

## File Structure After Setup

```
react-native-demo/
├── android/          # Generated by react-native init
├── ios/              # Generated by react-native init
├── src/              # Our demo source code ✓
│   ├── App.tsx
│   ├── screens/
│   └── store/
├── index.js          # App entry point ✓
├── app.json          # App config ✓
├── package.json      # Dependencies ✓
└── metro.config.js   # Metro bundler config ✓
```

## Troubleshooting

### Metro bundler issues
```bash
npm start -- --reset-cache
```

### iOS build issues
```bash
cd ios && pod install --repo-update && cd ..
```

### Android build issues
```bash
cd android && ./gradlew clean && cd ..
```
